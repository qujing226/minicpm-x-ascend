[project]
name = "minicpm"
version = "0.1.0"
description = "MiniCPM-o 4.5 inference baseline on Ascend with llama.cpp-omni"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "modelscope>=1.38.1",
]

[[tool.uv.index]]
url = "https://mirrors.aliyun.com/pypi/simple/"
default = true
