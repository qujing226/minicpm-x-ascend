[project]
name = "minicpm-x-ascend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "qujing226", email = "qujing226@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "stepaudio2-minicpmo>=0.1.1",
]

[project.scripts]
minicpm-x-ascend = "minicpm_x_ascend:main"

[[tool.uv.index]]
url = "https://mirrors.aliyun.com/pypi/simple/"
default = true

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
